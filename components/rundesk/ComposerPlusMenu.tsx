import { Clapperboard, FilePlus2, ListPlus, Mic, PanelsTopLeft, Paperclip, Share2, SquareCheckBig } from "lucide-react";
export default function ComposerPlusMenu({ onAction }: { onAction:(action:"upload"|"task"|"doc"|"whiteboard"|"clip"|"voice"|"share-task"|"share-channel")=>void }) { const items=[[Paperclip,"Upload file","upload"],[ListPlus,"Create task","task"],[FilePlus2,"Create Doc","doc"],[PanelsTopLeft,"Create Whiteboard","whiteboard"],[Clapperboard,"Record Clip","clip"],[Mic,"Record voice","voice"],[SquareCheckBig,"Share task","share-task"],[Share2,"Share channel item","share-channel"]] as const; return <section className="popover plus-menu">{items.map(([Icon,label,action])=><button key={action} onClick={()=>onAction(action)}><Icon size={17}/><span>{label}</span>{(action==="doc"||action==="voice")&&<small>Soon</small>}</button>)}</section>; }
